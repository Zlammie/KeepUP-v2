<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BuildRootz Community Requests</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/assets/css/main.css" />
  </head>
  <body>
    <%- include('../../partials/nav') %>
    <main class="main-content p-4">
      <header class="mb-3">
        <p class="text-uppercase text-muted small mb-1">BuildRootz</p>
        <h1 class="h4 mb-1">Community Creation Requests</h1>
        <p class="text-muted mb-0">Review pending requests and approve, link, or reject.</p>
      </header>

      <div class="card shadow-sm">
        <div class="card-body">
          <div class="table-responsive">
            <table class="table align-middle mb-0">
              <thead>
                <tr>
                  <th>Requested</th>
                  <th>Location</th>
                  <th>KeepUp Community</th>
                  <th>Status</th>
                  <th>Submitted</th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% if ((requests || []).length === 0) { %>
                  <tr>
                    <td colspan="6" class="text-muted text-center py-4">No requests found.</td>
                  </tr>
                <% } %>
                <% (requests || []).forEach((req) => { %>
                  <tr>
                    <td>
                      <div class="fw-semibold"><%= req.requestedName %></div>
                      <div class="text-muted small"><%= req.notes || '' %></div>
                    </td>
                    <td><%= [req.city, req.state].filter(Boolean).join(', ') %></td>
                    <td>
                      <% if (req.community) { %>
                        <div class="fw-semibold"><%= req.community.name %></div>
                        <div class="text-muted small"><%= [req.community.city, req.community.state].filter(Boolean).join(', ') %></div>
                      <% } else { %>
                        <span class="text-muted">Unknown</span>
                      <% } %>
                    </td>
                    <td><span class="badge text-bg-light"><%= req.status %></span></td>
                    <td class="text-muted small"><%= new Date(req.submittedAt).toLocaleString() %></td>
                    <td><a class="btn btn-sm btn-outline-primary" href="/admin/buildrootz/community-requests/<%= req.id %>">Review</a></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
