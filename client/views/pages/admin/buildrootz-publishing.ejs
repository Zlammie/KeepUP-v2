<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BuildRootz Publishing</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/assets/css/main.css" />
    <link rel="stylesheet" href="/assets/css/pages/buildrootz-publishing.css" />
  </head>
  <body>
    <%- include('../../partials/nav') %>
    <%- include('../../partials/top-nav-buildrootz') %>

    <main class="main-content p-4 brz-publishing-page">
      <header class="mb-4">
        <p class="text-uppercase text-muted small mb-1">BuildRootz</p>
        <h1 class="h4 mb-1">BuildRootz Publishing</h1>
        <p class="text-muted mb-0">
          KeepUp is the source of truth. Publish sends an internal bundle to BuildRootz with canonical mapped IDs.
        </p>
      </header>

      <section class="card shadow-sm mb-4">
        <div class="card-body">
          <div class="d-flex align-items-center justify-content-between mb-3">
            <h2 class="h6 mb-0">Builder Profile</h2>
            <small class="text-muted" id="brzProfileStatus"></small>
          </div>
          <form id="brzProfileForm" class="row g-3">
            <div class="col-md-6">
              <label class="form-label" for="brzBuilderName">Builder Name</label>
              <input id="brzBuilderName" class="form-control" type="text" readonly />
            </div>
            <div class="col-md-6">
              <label class="form-label" for="brzBuilderSlug">Builder Slug</label>
              <input id="brzBuilderSlug" class="form-control" type="text" placeholder="builder-slug" />
            </div>
            <div class="col-md-6">
              <label class="form-label" for="brzDisplayNameOverride">Display Name Override</label>
              <input id="brzDisplayNameOverride" class="form-control" type="text" placeholder="Optional display name override" />
            </div>
            <div class="col-md-6">
              <label class="form-label" for="brzCtaWebsite">CTA Website Link</label>
              <input id="brzCtaWebsite" class="form-control" type="url" placeholder="Falls back to Admin > BuildRootz website URL" />
            </div>
            <div class="col-md-6">
              <label class="form-label" for="brzCtaSchedule">CTA Schedule Link</label>
              <input id="brzCtaSchedule" class="form-control" type="url" placeholder="https://example.com/schedule" />
            </div>
            <div class="col-md-6">
              <label class="form-label" for="brzCtaContact">CTA Contact Link</label>
              <input id="brzCtaContact" class="form-control" type="url" placeholder="https://example.com/contact" />
            </div>
            <div class="col-md-6">
              <label class="form-label" for="brzShortDescription">Short Description</label>
              <textarea id="brzShortDescription" class="form-control" rows="3" placeholder="Short summary for builder profile"></textarea>
              <small class="text-muted">If empty, this uses the Admin BuildRootz short description.</small>
            </div>
            <div class="col-md-6">
              <label class="form-label" for="brzLongDescription">Long Description</label>
              <textarea id="brzLongDescription" class="form-control" rows="3" placeholder="Long-form builder profile content"></textarea>
            </div>
            <div class="col-12">
              <label class="form-label" for="brzPricingDisclaimer">Pricing Disclaimer (optional)</label>
              <textarea id="brzPricingDisclaimer" class="form-control" rows="2" placeholder="Prices, plans, and features subject to change without notice. Buyer to verify all information."></textarea>
            </div>
            <div class="col-12">
              <label class="form-label">Hero Image</label>
              <div class="d-flex flex-wrap align-items-center gap-3">
                <img id="brzHeroPreview" class="brz-image-preview d-none" alt="Builder hero image" />
                <input id="brzHeroUpload" type="file" class="form-control form-control-sm brz-upload-input" accept="image/*" />
                <button type="button" class="btn btn-outline-secondary btn-sm" id="brzHeroUploadBtn">Upload Hero</button>
              </div>
            </div>
            <div class="col-12 d-flex justify-content-end">
              <button type="submit" class="btn btn-primary" id="brzProfileSaveBtn">Save Builder Profile</button>
            </div>
          </form>
        </div>
      </section>

      <section class="card shadow-sm mb-4">
        <div class="card-body">
          <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
            <div class="d-flex align-items-center gap-2 flex-wrap">
              <h2 class="h6 mb-0">Communities</h2>
              <button type="button" class="btn btn-sm btn-outline-warning" id="brzBulkSyncBtn">
                Sync all out-of-date (<span id="brzBulkSyncCount">0</span>)
              </button>
            </div>
            <div class="btn-group btn-group-sm" role="group" aria-label="Community filters">
              <button type="button" class="btn btn-outline-secondary brz-community-filter active" data-filter="all">All</button>
              <button type="button" class="btn btn-outline-secondary brz-community-filter" data-filter="included">Included</button>
              <button type="button" class="btn btn-outline-secondary brz-community-filter" data-filter="missing-required">Missing required</button>
              <button type="button" class="btn btn-outline-secondary brz-community-filter" data-filter="missing-hero">Missing hero</button>
              <button type="button" class="btn btn-outline-secondary brz-community-filter" data-filter="missing-model">Missing model</button>
              <button type="button" class="btn btn-outline-secondary brz-community-filter" data-filter="missing-community-price">Missing community price</button>
            </div>
          </div>
          <div class="small text-muted mb-2" id="brzBulkSyncStatus"></div>
          <div class="table-responsive">
            <table class="table table-sm align-middle">
              <thead>
                <tr>
                  <th>Community</th>
                  <th>Include</th>
                  <th>Description Override</th>
                  <th>Completeness</th>
                  <th>Linked Floor Plans</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody id="brzCommunitiesBody">
                <tr>
                  <td colspan="6" class="text-muted">Loading communities...</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </section>

      <section class="card shadow-sm mb-4">
        <div class="card-body">
          <h2 class="h6 mb-1">Floor Plans (Catalog)</h2>
          <p class="small text-muted mb-3">Default Base Price (fallback): if a community-specific price is set, that community value is used instead.</p>
          <div class="table-responsive">
            <table class="table table-sm align-middle">
              <thead>
                <tr>
                  <th>Plan</th>
                  <th>Community</th>
                  <th>Include</th>
                  <th>Description Override</th>
                  <th>Default Base Price (fallback)</th>
                  <th>Primary Image</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody id="brzFloorPlansBody">
                <tr>
                  <td colspan="7" class="text-muted">Loading floor plans...</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </section>

      <section class="card shadow-sm mb-4">
        <div class="card-body">
          <h2 class="h6 mb-3">Publish / History</h2>
          <div class="row g-3 mb-3">
            <div class="col-md-6">
              <div class="border rounded p-3 h-100">
                <div class="d-flex justify-content-between align-items-center mb-2">
                  <div class="fw-semibold">Package</div>
                  <button class="btn btn-primary btn-sm" id="brzPublishPackageBtn" type="button">Publish Package</button>
                </div>
                <div class="small text-muted">Last Status</div>
                <div class="fw-semibold" id="brzPackagePublishVersion">-</div>
                <div class="small text-muted mt-2">Published At</div>
                <div class="fw-semibold" id="brzPackagePublishAt">-</div>
                <div class="small mt-2" id="brzPackagePublishStatus"></div>
              </div>
            </div>
            <div class="col-md-6">
              <div class="border rounded p-3 h-100">
                <div class="d-flex justify-content-between align-items-center mb-2">
                  <div class="fw-semibold">Inventory</div>
                  <button class="btn btn-outline-primary btn-sm" id="brzPublishInventoryBtn" type="button">Publish Inventory</button>
                </div>
                <div class="small text-muted">Last Status</div>
                <div class="fw-semibold" id="brzInventoryPublishVersion">-</div>
                <div class="small text-muted mt-2">Published At</div>
                <div class="fw-semibold" id="brzInventoryPublishAt">-</div>
                <div class="small mt-2" id="brzInventoryPublishStatus"></div>
                <div class="small mt-2 brz-warning-list d-none" id="brzInventoryWarnings"></div>
              </div>
            </div>
          </div>
          <div class="d-flex justify-content-end">
            <a class="btn btn-outline-secondary btn-sm" id="brzPreviewLink" href="#" target="_blank" rel="noopener noreferrer">Open Public Preview</a>
          </div>
          <div class="small mt-2" id="brzPublishStatus"></div>
        </div>
      </section>
    </main>

    <script src="/public/js/admin/buildrootz-publishing.js" nonce="<%= cspNonce %>" defer></script>
  </body>
</html>
