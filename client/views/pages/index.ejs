<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Simple CRM</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <!-- single entry-point for all your app's CSS -->
  <link rel="stylesheet" href="/assets/css/main.css">
</head>
<body>
  <!-- include navbar HTML at the top of every page -->
  <%- include('../partials/nav') %>
  <%- include('../partials/top-nav-blank') %>

  <main class="container py-4" id="dashboard-home">
    <div class="row g-4 mb-4 align-items-stretch">
      <div class="col-lg-8">
        <div class="card shadow-sm h-100">
          <div class="card-body d-flex flex-column gap-4">
            <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start gap-4">
              <div>
                <p class="text-uppercase text-muted small mb-1">Welcome back</p>
                <h1 class="h3 mb-1" id="dashboard-user-name">
                  <%= (currentUser && currentUser.name) || 'Guest' %>
                </h1>
                <p class="text-muted mb-0" id="dashboard-user-company">
                  Company: <%= (currentUser && currentUser.company) || 'N/A' %>
                </p>
              </div>
              <div class="w-100 w-lg-auto" style="max-width: 280px;">
                <label for="dashboard-community-select" class="form-label text-muted text-uppercase small mb-1">
                  Community View
                </label>
                <select class="form-select form-select-sm" id="dashboard-community-select"
                  data-user-id="<%= currentUser ? currentUser.id : '' %>"
                  data-active-community="<%= currentUser && currentUser.defaultCommunityId ? currentUser.defaultCommunityId : 'all' %>">
                  <option value="all">All Communities</option>
                </select>
                <small class="text-muted d-block mt-1">Switching filters the stats below.</small>
              </div>
            </div>

            <div>
              <p class="text-uppercase text-muted small mb-2">Quick Actions</p>
              <div class="d-flex flex-wrap gap-2" id="dashboard-quick-actions">
                <a class="btn btn-primary" href="/add-lead">Add Contact</a>
                <a class="btn btn-outline-secondary" href="/task">Open Tasks</a>
                <a class="btn btn-outline-secondary" href="/community-management">Manage Communities</a>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="col-lg-4">
        <div class="card shadow-sm h-100" id="weather-card">
          <div class="card-body d-flex flex-column justify-content-between">
            <div class="d-flex justify-content-between align-items-start">
              <div>
                <p class="text-uppercase text-muted small mb-1">Weather</p>
                <h2 class="display-6 mb-0" id="weather-temp">--°</h2>
                <p class="mb-0 text-muted" id="weather-summary">Fetching conditions…</p>
              </div>
              <div class="text-end">
                <span class="fs-1" id="weather-icon">☀️</span>
              </div>
            </div>
            <div class="mt-3">
              <p class="mb-0 text-muted small" id="weather-location">Location unavailable</p>
              <p class="mb-0 text-muted small" id="weather-updated">Updated --:--</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="row g-4 mb-4">
      <div class="col-xl-5">
        <div class="card shadow-sm h-100" id="weekly-traffic-card">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-2">
              <div>
                <h2 class="h5 mb-0">Weekly Traffic</h2>
                <small class="text-muted">Mon – Sun</small>
              </div>
              <div class="text-end">
                <span class="text-muted small d-block">Current week</span>
                <span class="fw-semibold" id="weekly-traffic-range">-- / --</span>
              </div>
            </div>
            <p class="text-muted small">Includes leads, realtors, and be-backs with visit dates.</p>
            <div class="d-flex flex-wrap gap-3">
              <div class="flex-fill text-center border rounded p-3">
                <p class="text-muted text-uppercase small mb-1">Contacts</p>
                <p class="display-6 mb-0" id="weekly-traffic-contacts">0</p>
              </div>
              <div class="flex-fill text-center border rounded p-3">
                <p class="text-muted text-uppercase small mb-1">Realtors</p>
                <p class="display-6 mb-0" id="weekly-traffic-realtors">0</p>
              </div>
              <div class="flex-fill text-center border rounded p-3">
                <p class="text-muted text-uppercase small mb-1">Be-Backs</p>
                <p class="display-6 mb-0" id="weekly-traffic-bebacks">0</p>
              </div>
            </div>
            <div class="mt-3">
              <canvas id="weekly-traffic-sparkline" height="90" role="img" aria-label="Weekly traffic sparkline"></canvas>
            </div>
          </div>
        </div>
      </div>

      <div class="col-xl-4">
        <div class="card shadow-sm h-100" id="recent-sales-card">
          <div class="card-body d-flex flex-column">
            <div class="d-flex justify-content-between align-items-center mb-3">
              <h2 class="h5 mb-0">Recent Sales</h2>
              <span class="badge bg-light text-dark" id="recent-sales-count">0</span>
            </div>
            <ul class="list-group list-group-flush flex-grow-1" id="recent-sales-list">
              <li class="list-group-item d-flex justify-content-between align-items-start text-muted">
                <div>
                  <p class="fw-semibold mb-1">No sales logged</p>
                  <small>Once sales are recorded they’ll appear here.</small>
                </div>
              </li>
            </ul>
            <template id="recent-sale-template">
              <li class="list-group-item d-flex justify-content-between align-items-center gap-3">
                <div>
                  <p class="fw-semibold mb-0" data-sale-contact>Contact Name</p>
                  <small class="text-muted d-block" data-sale-lot>Lot / Community</small>
                </div>
                <div class="btn-group btn-group-sm">
                  <a class="btn btn-outline-primary" data-sale-contact-link href="#">Contact</a>
                  <a class="btn btn-outline-secondary" data-sale-lot-link href="#">Lot</a>
                </div>
              </li>
            </template>
          </div>
        </div>
      </div>

      <div class="col-xl-3">
        <div class="card shadow-sm h-100 text-center" id="lot-count-card">
          <div class="card-body d-flex flex-column justify-content-center">
            <p class="text-uppercase text-muted small mb-1">Total Lots</p>
            <p class="display-4 mb-2" id="total-lot-count">0</p>
            <p class="text-muted mb-0">Across active communities</p>
          </div>
        </div>
      </div>
    </div>

    <div class="row g-4">
      <div class="col-xxl-8">
        <div class="card shadow-sm h-100" id="monthly-traffic-card">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-3">
              <div>
                <h2 class="h5 mb-0">Traffic This Month</h2>
                <small class="text-muted">Daily breakdown</small>
              </div>
              <div class="btn-group btn-group-sm" role="group" aria-label="Traffic range">
                <button type="button" class="btn btn-outline-secondary active" data-traffic-range="month">Month</button>
                <button type="button" class="btn btn-outline-secondary" data-traffic-range="quarter">Quarter</button>
              </div>
            </div>
            <canvas id="monthly-traffic-chart" height="260" role="img" aria-label="Monthly traffic chart"></canvas>
          </div>
        </div>
      </div>

      <div class="col-xxl-4">
        <div class="card shadow-sm h-100" id="recent-leads-card">
          <div class="card-body d-flex flex-column">
            <div class="d-flex justify-content-between align-items-center mb-3">
              <h2 class="h5 mb-0">Recent Leads</h2>
              <a class="btn btn-sm btn-outline-secondary" href="/contacts">View All</a>
            </div>
            <div class="table-responsive flex-grow-1">
              <table class="table table-sm align-middle mb-0">
                <thead class="text-muted small">
                  <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Status</th>
                    <th scope="col">Visit</th>
                  </tr>
                </thead>
                <tbody id="recent-leads-tbody">
                  <tr class="text-muted">
                    <td colspan="3">No leads loaded yet.</td>
                  </tr>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
