<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Reset password · KeepUP</title>
  <link rel="preconnect" href="/" />
  <link rel="stylesheet" href="/assets/css/auth.css" />
</head>
<body class="auth no-nav">
  <main class="auth__wrap">
    <section class="auth__card" role="form" aria-labelledby="forgot-title">
      <header class="auth__header">
        <h1 id="forgot-title" class="auth__title">Reset your password</h1>
        <p class="auth__subtitle">We will send a link to the email on your account.</p>
      </header>

      <% if (error || message) { %>
        <% const isSuccess = status === 'success'; %>
        <div class="auth__alert <%= isSuccess ? 'auth__alert--success' : '' %>" role="alert" aria-live="polite">
          <span class="auth__alert-dot" aria-hidden="true"></span>
          <%= error || message %>
        </div>
      <% } %>

      <form method="POST" action="/forgot-password" class="auth__form">
        <div class="field">
          <label for="email" class="label">Email</label>
          <input
            id="email"
            name="email"
            type="email"
            required
            autocomplete="email"
            class="input"
            placeholder="you@company.com"
            value="<%= values?.email || '' %>"
          />
        </div>

        <button type="submit" class="btn btn--primary">Send reset link</button>
      </form>

      <footer class="auth__footer">
        <a href="/login" class="link">Back to sign in</a>
      </footer>
    </section>
  </main>
</body>
</html>
